# GitHub Pages 설정 가이드

## 🎯 목적

GitHub Pages가 GitHub Actions 워크플로우를 사용하여 배포되도록 설정하는 방법을 안내합니다.

## 📋 설정 단계

### 1단계: GitHub 저장소 접속

1. 브라우저에서 GitHub 저장소 접속
   - URL: https://github.com/laguyo87/seating-arrangement-program

### 2단계: Settings 탭 열기

1. 저장소 상단의 **"Settings"** 탭 클릭
2. 왼쪽 사이드바에서 **"Pages"** 클릭

### 3단계: Source 설정 확인 및 변경

**현재 설정 확인:**
- "Source" 섹션을 확인합니다
- 현재 선택된 옵션을 확인합니다

**올바른 설정:**
- ✅ **"GitHub Actions"** 선택되어 있어야 함

**설정 변경 방법 (필요시):**
1. "Source" 드롭다운 메뉴 클릭
2. **"GitHub Actions"** 선택
3. **"Save"** 버튼 클릭

## 🔍 설정 옵션 설명

### 옵션 1: "Deploy from a branch"
- **의미**: 특정 브랜치의 파일을 직접 배포
- **사용 시기**: 정적 파일만 있는 경우
- **현재 프로젝트**: ❌ 사용하지 않음 (빌드가 필요함)

### 옵션 2: "GitHub Actions" (권장)
- **의미**: GitHub Actions 워크플로우를 통해 배포
- **사용 시기**: 빌드가 필요한 프로젝트 (TypeScript 등)
- **현재 프로젝트**: ✅ 사용해야 함

## ⚠️ 주의사항

### 설정 변경 시
- 설정을 변경하면 기존 배포가 취소될 수 있습니다
- 새로운 배포가 자동으로 시작됩니다
- 배포 완료까지 2-5분 소요됩니다

### 권한 확인
- 저장소에 대한 관리자 권한이 필요합니다
- 권한이 없으면 설정을 변경할 수 없습니다

## ✅ 설정 확인

### 올바르게 설정되었는지 확인하는 방법

1. **Settings → Pages**에서 확인
   - "Source"가 "GitHub Actions"로 표시되어야 함

2. **Actions 탭에서 확인**
   - 워크플로우가 실행되고 있는지 확인
   - 성공적으로 배포되고 있는지 확인

3. **Deployments 탭에서 확인**
   - 배포가 정상적으로 진행되고 있는지 확인

## 🔧 문제 해결

### "GitHub Actions" 옵션이 보이지 않는 경우

**원인:**
- GitHub Pages가 아직 활성화되지 않았을 수 있음
- 저장소가 비공개일 수 있음 (GitHub Pages는 공개 저장소에서만 무료)

**해결 방법:**
1. 저장소를 공개로 변경 (Settings → General → Danger Zone)
2. 또는 GitHub Pro 계정 사용

### 설정을 변경했는데도 배포가 실패하는 경우

**확인 사항:**
1. 워크플로우 파일이 올바른지 확인 (`.github/workflows/deploy.yml`)
2. 워크플로우 권한이 올바른지 확인
3. GitHub Actions 로그에서 에러 확인

## 📝 현재 설정 상태

**권장 설정:**
- Source: **GitHub Actions**
- Branch: (자동 설정됨)
- Custom domain: (선택사항)

**확인 방법:**
- Settings → Pages에서 현재 설정 확인
- 위 설정과 일치하는지 확인

## 🎯 다음 단계

설정이 완료되면:
1. GitHub Actions 워크플로우가 자동으로 실행됩니다
2. 2-5분 후 배포가 완료됩니다
3. 사이트가 업데이트됩니다

자세한 내용은 `NEXT_STEPS.md` 파일을 참고하세요.

